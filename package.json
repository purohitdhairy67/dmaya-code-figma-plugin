{
  "name": "dmaya-code-figma-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "dMaya HTML-to-Figma importer plugin.",
  "scripts": {
    "syntax": "node --check code.js",
    "check": "node scripts/release-check.mjs",
    "pack": "npm run check && node scripts/pack-plugin.mjs"
  },
  "devDependencies": {}
}
